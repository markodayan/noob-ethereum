// transaction types (0x0, 0x1, 0x2)

// Correct RLP calcs (legacy txs)
// - ETH Transfer
// - Contract call (ETH sent to contract along with message call)

// Incorrect RLP calcs (legacy txs)
// - Contract calls (no ETH sent with message call)
